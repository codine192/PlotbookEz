<Page
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:EzPlot.Views" 
      xmlns:control="clr-namespace:EzPlot.Controls" 
      xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls" x:Class="EzPlot.Views.MainPage" 
      x:Name="Self"
      mc:Ignorable="d" 
      
      Title="MainPage">


    <Border BorderBrush="CornflowerBlue" BorderThickness="1">
        <Grid DataContext="{Binding ElementName=Self}" Height="550" Width="850">
            <ScrollViewer HorizontalScrollBarVisibility="Auto">
            <Canvas x:Name="OverlayCanvas"  MouseLeftButtonDown="Canvas_MouseLeftButtonDown" Background="Azure" MaxWidth="2000" MaxHeight="2000" MinHeight="500" MinWidth="800">
                    <Canvas.LayoutTransform>
                        <ScaleTransform ScaleX="{Binding ElementName=ZoomSlider, Path=Value}" 
                                    ScaleY="{Binding ElementName=ZoomSlider, Path=Value}"/>
                    </Canvas.LayoutTransform>
                <Image x:Name="mapImage" Stretch="UniformToFill" MinHeight="550" MinWidth="850"/> 
                
            </Canvas>
            </ScrollViewer>
            <Slider Name="ZoomSlider" Minimum="1" Maximum="10" Value="1" Orientation="Vertical" Height="200" Width="20" HorizontalAlignment="Left" mah:SliderHelper.ThumbFillBrush="#FF0AABE2"/>
            <control:MarkerToolBoxControl x:Name="markerToolBoxControl" VerticalAlignment="Top" />
        </Grid>
    </Border>


</Page>
